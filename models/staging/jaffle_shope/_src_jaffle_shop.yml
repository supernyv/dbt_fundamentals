sources :
  - name : jaffle_shop
    database : raw
    schema : jaffle_shop
    tables:
    -   name : customer
    -   name : customer_order
        config :
          freshness :
            warn_after : {count : 24, period : hour} #Warning if latest data is older than 24 hours
            error_after : {count : 48, period : hour} #Error if latest data is older than 48 hours
          loaded_at_field : _etl_loaded_at